# Тренировочные задания по JavaScript объектам 🎯

Привет! Эти задания созданы для практического изучения работы с объектами на реальных данных из проекта панели управления банковскими картами.

## 📁 Структура файлов

- **`README.md`** (этот файл) - общая инструкция
- **`data.js`** - тренировочные данные (карты, транзакции, пользователи)
- **`tasks.js`** - 27 заданий с прототипами функций
- **`DESCRIPTION.md`** - подробное описание каждого уровня и концепций
- **`hints.js`** - примеры решений (не смотрите пока не попробуете!)

## 🚀 Как начать

### Шаг 1: Установите зависимости

```bash
# Если еще не установлены зависимости проекта
npm install
```

### Шаг 2: Изучите данные

Откройте файл `data.js` и посмотрите на структуру данных:
- Карты пользователей с балансами и лимитами
- Банковские продукты (bin) 
- Транзакции
- Профиль пользователя с настройками
- События системы

### Шаг 3: Прочитайте описания

Откройте `DESCRIPTION.md` и прочитайте о:
- Базовых операциях с объектами
- **Копировании объектов** (крайне важно!)
- Работе с массивами
- Группировке данных

### Шаг 4: Начните решать задания

Откройте `tasks.js` - там 27 функций с прототипами. Ваша задача - реализовать каждую функцию.

Начните с Уровня 1 (задания 1-3) - они самые простые.

## 📝 Как решать задания

### Для каждого задания:

1. **Прочитайте описание** в комментарии к функции
2. **Посмотрите на типы** параметров и возвращаемого значения
3. **Изучите примеры данных** из `data.js`
4. **Напишите решение** вместо `// TODO: ...`
5. **Проверьте** что функция не мутирует исходные данные

### Пример работы

**Задание 1**: Получить имя карты

```javascript
// До реализации:
export function getCardName(card) {
  // TODO: Реализовать получение имени карты
  return "";
}

// После реализации:
export function getCardName(card) {
  return card.name;
}
```

### Тестирование решений

// TODO: Тут нужно добавить описание про то что будет

## ⚠️ Важные правила

### 1. Не мутируйте исходные данные!

❌ **ПЛОХО**:
```javascript
function renameCard(card, newName) {
  card.name = newName; // Изменили оригинал!
  return card;
}
```

✅ **ХОРОШО**:
```javascript
function renameCard(card, newName) {
  return { ...card, name: newName }; // Создали новую копию
}
```

### 2. Используйте оператор спреда

Для копирования объектов:
```javascript
const copy = { ...original };
```

Для копирования массивов:
```javascript
const newArray = [...oldArray, newItem];
```

### 3. Проверяйте опциональные поля

Безопасный доступ к вложенным полям:
```javascript
const limit = card.limits?.daily; // Не упадет если limits нет

// Или с проверкой
if (card.limits && card.limits.daily) {
  // ...
}
```

### 4. Преобразуйте строки в числа при сравнениях

```javascript
// Строковое сравнение (ПЛОХО):
if (card.balance > "100") { ... } // "50.00" > "100" = true (ошибка!)

// Числовое сравнение (ХОРОШО):
if (parseFloat(card.balance) > 100) { ... }
```

## 📊 Уровни сложности

### Уровень 1 (Задания 1-3)
**Базовые операции**: доступ к полям, проверка значений


### Уровень 2 (Задания 4-6)
**Вложенные объекты**: работа с объектами внутри объектов


### Уровень 3 (Задания 7-10)
**Массивы**: filter, reduce, find


### Уровень 4 (Задания 11-14)
**Трансформация**: копирование, создание новых объектов

**Особое внимание**: здесь вы изучите копирование объектов!

### Уровень 5 (Задания 15-17)
**Группировка**: reduce для агрегации данных


### Уровень 6 (Задания 18-19)
**Глубокие вложенности**: безопасный доступ к глубоко вложенным полям


### Уровень 7 (Задания 20-22)
**Преобразование**: массивы → объекты, объединение


### Уровень 8 (Задания 23-24)
**Сложная фильтрация**: множественные условия


### Уровень 9 (Задания 25-27)
**Массивы в объектах**: неизменяемые обновления

## ✅ Когда задание решено правильно?

1. ✅ Функция компилируется без ошибок
2. ✅ Возвращает правильный тип данных
3. ✅ Не мутирует исходные данные
4. ✅ Работает с edge cases (пустые массивы, null, undefined)
5. ✅ Проходит ваши тесты

# Тестирование с Vitest

Этот проект использует **Vitest** для тестирования функций. Vitest предоставляет подробную информацию об ошибках, что помогает быстро понять, что именно реализовано неправильно.

## Запуск тестов

### Основные команды

```bash
# Запустить все тесты один раз
npm test

# Запустить тесты в режиме наблюдения (автоматически перезапускаются при изменении файлов)
npm run test:watch

# Запустить тесты с подробным выводом
npm run test:verbose

# Запустить интерактивный UI для тестирования
npm run test:ui
```

## Что показывает Vitest при ошибках?

Когда тест не проходит, Vitest показывает:

### 1. **Подробное сообщение об ошибке**
- Что ожидалось получить
- Что было получено на самом деле
- Разница между ожидаемым и полученным (diff)

### 2. **Стек вызовов**
- Где именно в вашем коде произошла ошибка
- Какая функция была вызвана
- Какая строка кода выполнилась

### 3. **Визуальное сравнение (diff)**
- Для объектов и массивов показывает, какие поля отличаются
- Помогает увидеть, какие значения неправильные

### 4. **Контекст теста**
- Какие входные данные использовались
- Какое условие проверялось

## Примеры ошибок

### Пример 1: Неправильное значение

Если функция `getCardName` возвращает пустую строку вместо имени:

```
✓ должен возвращать имя карты
  Expected: "Основная карта"
  Received: ""

  ❌ Ошибка: функция не извлекла поле `name` из объекта
```

### Пример 2: Неправильный тип данных

Если функция возвращает не тот тип:

```
✓ должен возвращать количество активных карт
  Expected: 2
  Received: "2"

  ❌ Ошибка: функция вернула строку вместо числа
```

### Пример 3: Различия в объектах

Если объект отличается от ожидаемого:

```
✓ должен создавать упрощенную копию карты
  Expected: { id: "1", name: "Основная карта", balance: "1500.50", currency: "USD" }
  Received: { id: "", name: "", balance: "", currency: "" }

  ❌ Ошибка: поля не были заполнены из исходного объекта
```


## Интерактивный UI

Запустите `npm run test:ui` чтобы открыть веб-интерфейс:
- Просматривайте результаты тестов в реальном времени
- Фильтруйте тесты по статусу
- Смотрите подробную информацию о каждом тесте
- Отлаживайте код прямо в браузере

## Советы для отладки

1. **Читайте сообщения об ошибках внимательно** - они точно показывают, что не так
2. **Используйте diff** - он показывает различия между ожидаемым и полученным
3. **Проверяйте типы данных** - часто ошибки из-за неправильных типов
4. **Смотрите на входные данные** - возможно, функция не обрабатывает какой-то случай
5. **Используйте режим watch** - тесты автоматически перезапускаются при изменениях

## Настройки диагностики

Проект настроен для максимально подробной диагностики:

- ✅ Подробные сообщения об ошибках (verbose reporter)
- ✅ Цветной вывод для лучшей читаемости
- ✅ Diff для объектов и массивов (до 75 строк)
- ✅ Полный стек вызовов
- ✅ Source maps для точного указания мест ошибок
- ✅ Покрытие кода для визуализации протестированных участков

Эти настройки помогают быстро находить и исправлять ошибки в реализации функций!
